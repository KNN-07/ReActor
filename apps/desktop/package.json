{
  "name": "@reactor/desktop",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": { "dev": "vite", "build": "vite build", "check": "biome check . && tsgo -p tsconfig.json --noEmit", "test": "bun test" },
  "dependencies": { "@reactor/shared-ui": "workspace:*", "@reactor/wire": "workspace:*", "react": "catalog:", "react-dom": "catalog:", "vite": "catalog:" },
  "devDependencies": { "@types/react": "catalog:", "@types/react-dom": "catalog:" }
}
