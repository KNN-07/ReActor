# Learn

Use the existing `learn` tool to record a reusable lesson from `$ARGUMENTS`.
If the argument names a workflow or source, summarize the durable lesson,
include the context that makes it reusable, and create or update a managed skill
when that would help future sessions. Do not store secrets or transient output.
